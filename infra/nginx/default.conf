server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # React Router 경로를 새로고침해도 SPA의 index.html로 돌려보냅니다.
  location / {
    try_files $uri $uri/ /index.html;
  }
}
