# 초기 화면 구성

## 역할별 진입 구조

초기 운영 화면은 관리자 페이지와 Agent 페이지로 분리한다. 두 영역은 URL과 권한 검사를 분리하며, 로그인한 역할과 다른 페이지에는 접근할 수 없어야 한다.

## 관리자 페이지

### Agent 관리

- Agent 목록 조회
- Agent 추가
- 기본 계정 상태 표시

### 호텔·룸 관리

- 호텔 추가
- 호텔 선택 필터
- 선택한 호텔에 룸 추가
- 룸 목록 테이블 조회

권장 룸 테이블 열은 `호텔`, `룸 번호`, `상태`, `생성일`, `QR 관리`다.

## QR 관리 — MVP 이후

룸 테이블 마지막 열의 QR 버튼에서 다음 기능을 제공한다.

- 룸별 QR 코드 생성
- QR 코드 미리보기
- 이미지 다운로드
- 기존 QR 삭제 또는 폐기
- 새 QR 재생성

QR 재생성 시 이전 접근 키는 즉시 비활성화하고, 새로운 접근 키를 발급해 이전 QR을 재사용할 수 없게 한다. 초기 MVP에서는 QR 기능을 구현하지 않으며 테스트용 링크 또는 접근 키를 사용한다.

## Agent 페이지

- Agent 로그인
- 대기·진행·종료 상담 목록
- 상담 선택 및 수락
- 투숙객과 실시간 채팅
- 객실 정보, 상담 상태, 시작 시각과 남은 시간 표시
- 상담 종료

### Agent 화면 디자인 원칙

- 투숙객 화면과 동일한 밝은 회색 배경, 흰색 카드, 파란색 주요 동작 색상을 사용한다.
- 채팅 말풍선은 Agent 발신을 파란색 오른쪽 말풍선, 투숙객 발신을 회색 왼쪽 말풍선으로 표시한다.
- 연결 상태는 초록색, 남은 시간은 주황색, 종료 동작은 빨간색으로 의미를 일관되게 구분한다.
- 상담 목록은 대기·진행·종료 요약 카드와 상태별 표를 사용하고, 640px 이하에서는 표를 항목명이 포함된 카드 목록으로 전환한다.
- 900px 이하 화면에서는 사이드바를 숨기며, 640px 이하에서는 세 개의 요약 카드를 한 줄의 간결한 지표로 표시한다.
- 모바일 채팅은 동적 뷰포트 전체를 사용하고 안전 영역을 반영해 주소창·가상 키보드가 나타나도 입력창을 유지한다.
- 새 메시지와 상담 종료 상태가 반영되면 채팅 영역 내부만 마지막 메시지로 이동하고 전체 페이지 스크롤은 변경하지 않는다.
- 고객 모바일 채팅을 우선 UI로 보고, 460px 이하에서는 바깥 카드·그림자를 제거한 전체화면 메신저 형태로 호텔·객실/연결 헤더, 말풍선 목록, 44px 이상 입력·전송 영역을 제공한다.
