{
  "name": "hotel-callcenter-chat",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.12.1",
  "scripts": {
    "dev": "pnpm --parallel --filter './apps/*' dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "db:generate": "pnpm --filter @hotel-chat/server prisma:generate",
    "db:migrate": "pnpm --filter @hotel-chat/server prisma:migrate",
    "db:seed": "pnpm --filter @hotel-chat/server prisma:seed"
  }
}
